apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: xpo-logistics
  labels:
    app.kubernetes.io/name: api
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app.kubernetes.io/name: api
