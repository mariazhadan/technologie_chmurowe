apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: xpo-logistics
  labels:
    app.kubernetes.io/name: postgres
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app.kubernetes.io/name: postgres
