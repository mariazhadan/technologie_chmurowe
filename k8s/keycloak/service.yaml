apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: xpo-logistics
  labels:
    app.kubernetes.io/name: keycloak
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: keycloak
