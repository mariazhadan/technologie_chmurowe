apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: xpo-logistics
  labels:
    app.kubernetes.io/name: frontend
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app.kubernetes.io/name: frontend
